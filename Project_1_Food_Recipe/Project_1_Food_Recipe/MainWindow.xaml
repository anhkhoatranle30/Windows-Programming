<Window x:Class="Project_1_Food_Recipe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_1_Food_Recipe"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1080"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    <Window.Icon>
        <BitmapImage UriSource="Images/icon.png" RenderOptions.BitmapScalingMode="HighQuality" />
    </Window.Icon>

    <Window.Resources>

        <Style x:Key="fontStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily"
                    Value="Font/Poppins-Medium.ttf #Poppins" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="FontSize"
                    Value="15" />
            <Setter Property="Margin"
                    Value="17" />
            <Setter Property="HorizontalAlignment"
                    Value="Center" />
            <Setter Property="VerticalAlignment"
                    Value="Bottom" />
        </Style>

        <Style x:Key="homeBtnStyle" TargetType="{x:Type Button}">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Width"
                    Value="100" />
            <Setter Property="Height"
                    Value="100" />
            <Setter Property="Margin"
                    Value="5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Border x:Name="border"
                                CornerRadius="40"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            </Border>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalAlignment}" />
                            <Grid x:Name="img" Width="40" Height="40" Margin="30,25,30,35" Background="White">
                                <Grid.OpacityMask>
                                    <ImageBrush ImageSource="Images/img_home_default.png" />
                                </Grid.OpacityMask>
                            </Grid>
                            <TextBlock x:Name="text" Text="Home" Style="{StaticResource fontStyle}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="White" />
                                <Setter TargetName="img" Property="Background" Value="#3498DB" />
                                <Setter TargetName="text" Property="Foreground" Value="#3498DB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">

                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="7" ShadowDepth="7" Color="#808080" Direction="300" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="favoriteBtnStyle" TargetType="{x:Type Button}">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Width"
                    Value="100" />
            <Setter Property="Height"
                    Value="100" />
            <Setter Property="Margin"
                    Value="5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Border x:Name="border"
                                CornerRadius="40"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            </Border>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalAlignment}" />
                            <Grid x:Name="img" Width="40" Height="40" Margin="30,25,30,35" Background="White">
                                <Grid.OpacityMask>
                                    <ImageBrush ImageSource="Images/img_favorite_default.png" />
                                </Grid.OpacityMask>
                            </Grid>
                            <TextBlock x:Name="text" Text="Favorite" Style="{StaticResource fontStyle}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="White" />
                                <Setter TargetName="img" Property="Background" Value="#3498DB" />
                                <Setter TargetName="text" Property="Foreground" Value="#3498DB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">

                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="7" ShadowDepth="7" Color="#808080" Direction="300" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="addBtnStyle" TargetType="{x:Type Button}">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Width"
                    Value="100" />
            <Setter Property="Height"
                    Value="100" />
            <Setter Property="Margin"
                    Value="5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Border x:Name="border"
                                CornerRadius="40"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            </Border>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalAlignment}" />
                            <Grid x:Name="img" Width="40" Height="40" Margin="30,25,30,35" Background="White">
                                <Grid.OpacityMask>
                                    <ImageBrush ImageSource="Images/img_add_default.png" />
                                </Grid.OpacityMask>
                            </Grid>
                            <TextBlock x:Name="text" Text="Add" Style="{StaticResource fontStyle}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="White" />
                                <Setter TargetName="img" Property="Background" Value="#3498DB" />
                                <Setter TargetName="text" Property="Foreground" Value="#3498DB" />
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">

                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="7" ShadowDepth="7" Color="#808080" Direction="300" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="settingBtnStyle" TargetType="{x:Type Button}">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Width"
                    Value="100" />
            <Setter Property="Height"
                    Value="100" />
            <Setter Property="Margin"
                    Value="5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Border x:Name="border"
                                CornerRadius="40"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            </Border>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalAlignment}" />
                            <Grid x:Name="img" Width="40" Height="40" Margin="30,25,30,35" Background="White">
                                <Grid.OpacityMask>
                                    <ImageBrush ImageSource="Images/img_setting_default.png" />
                                </Grid.OpacityMask>
                            </Grid>
                            <TextBlock x:Name="text" Text="Setting" Style="{StaticResource fontStyle}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="White" />
                                <Setter TargetName="img" Property="Background" Value="#3498DB" />
                                <Setter TargetName="text" Property="Foreground" Value="#3498DB" />
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">

                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="7" ShadowDepth="7" Color="#808080" Direction="300" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="40" Background="#3498DB">

        <Grid>
            <WrapPanel>
                <Grid Width="120" Height="800">
                    <Border Width="60" Height="60" CornerRadius="10" Background="White" Canvas.Left="30" Canvas.Top="19" Margin="29,46,31,694">
                        <Canvas>
                            <Image Source="Images/img_user.png" Height="60" Width="60" />
                            <TextBlock Style="{StaticResource fontStyle}" Text="Hi User!" Canvas.Top="81" Foreground="White" FontSize="23" Margin="0" Canvas.Left="-13" />
                        </Canvas>
                    </Border>

                    <Grid Width="120" Height="450" Canvas.Top="210">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="9*" />
                        </Grid.RowDefinitions>

                        <StackPanel Margin="0,-1,0,-32.4" Grid.RowSpan="2">

                            <Button x:Name="homeBtn" Style="{StaticResource homeBtnStyle}"  Click="homeBtn_Click" />
                            <Button x:Name="favoriteBtn" Style="{StaticResource favoriteBtnStyle}"  Click="favoriteBtn_Click" />
                            <Button x:Name="addBtn" Style="{StaticResource addBtnStyle}"  Click="addBtn_Click" />
                            <Button x:Name="settingBtn" Style="{DynamicResource settingBtnStyle}" Click="settingBtn_Click" />
                        </StackPanel>
                    </Grid>
                </Grid>
                <Border Background="White" Height="800" Width="960" CornerRadius="40">
                    <Grid Margin="50,30">

                        <Grid x:Name="homeScreen" Margin="2,2,-2,-2">
                            <TextBox x:Name="title" Width="500" Height="40" Margin="44,42,316,658">
                            </TextBox>
                            <TextBox x:Name="description" AcceptsReturn="True" TextWrapping="Wrap" Width="500" Height="100" Margin="44,134,316,506">
                            </TextBox>

                            <TextBox x:Name="linkYt" Width="500" Height="40" Margin="44,319,316,381">
                            </TextBox>
                        </Grid>
                        <Grid x:Name="favoriteScreen">
                        </Grid>

                        <Grid x:Name="addScreen">
                        </Grid>

                        <Grid x:Name="settingScreen">
                        </Grid>

                        <Button Name="shuwdownBtn" Click="shuwdownBtn_Click" Width="40" Height="40" BorderThickness="0" Background="Transparent" Margin="832,10,-12,690">
                            <Image Source="Images/img_shutdown.png" />
                        </Button>
                    </Grid>
                </Border>
            </WrapPanel>
        </Grid>
    </Border>
</Window>